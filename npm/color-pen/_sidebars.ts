import { createSidebarItem } from '../..//docusaurus-config/sidebar.item.config';

export const ColorPenSidebar = createSidebarItem({
  label: 'color pen',
  prefix: 'color-pen',
  items: ['static'],
});
