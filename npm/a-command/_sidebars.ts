import { createSidebarItem } from '../../docusaurus-config/sidebar.item.config';

export const ACommandSidebar = createSidebarItem({
  prefix: 'a-command',
  label: 'a command（终端工具）',
  items: ['args', 'question', 'selection'],
});
