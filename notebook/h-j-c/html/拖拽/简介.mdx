---
title: 简介
pagination_prev: null

---

# 拖拽 API

拖放 API 实际上有两个部分：拖拽（ Drag ）和释放（ Drop ），拖拽是指鼠标点按源对象后一直到不放手，一旦放手即是释放。

- 源对象：指鼠标点按的一个事务，如一张图片、一个 DIV 、一段文本。
- 目标对象： 指拖动源对象后移动到一片区域，源对象可以进入这个区域，可以在这个区域上方悬停（未松手），可以释放源对象，将其放置进目标对象（松手后），也可以悬停后离开该区域

            