---
title: 限定符


---

# 限定符

用 `+` 限定必须出现一次或连续多次。

`*` 元字符规定其前导字符必须在目标对象中出现零次或连续多次。

`?` 元字符规定其前导字符必须在目标对象中连续出现 0 次或 1 次。

用 `{n}` 限定连续出现的次数。

用 `{n,}` 限定至少出现的次数。

用 `{n,m}` 限定最少与最多出现的次数。

            