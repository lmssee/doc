---
title: 简介
pagination_prev: null

---

# RegExp

正则表达式（ regular expression ）是一种可以用于模式匹配和替换的强有力的工具，是由一系列普通字符和特殊字符组成的能明确描述文本字符的文字匹配模式。

正则表达式描述了一种字符串匹配的模式，既可以使用户通过使用一系列普通字符和特殊字符来构建能够明确描述文本字符串的匹配模式，又可以用来检查一个字符串是否含有某种子字符串、将匹配的子字符串做替换或者从某个字符串中取出符合某个条件的子字符串等。

一个正则表达式就是由普通字符（如字符 a 到 z ）以及特殊字符（称为元字符）组成的文字模式。该模式描述在查找文字主体时待匹配的一个或多个字符串。正则表达式作为一个模板，将某个字符模式与所搜索的字符串进行匹配。

正则表达式是一种可以用于模式匹配和替换的强有力的工具。

- 测试字符串的某个模式
- 替换文本
- 根据模式匹配从字符串中提取一个子字符串

            