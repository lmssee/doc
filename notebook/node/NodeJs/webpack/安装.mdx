---
title: 安装


---

# npm

在目录下使用 npm init 初始化项目。 webpack 依托于 node.js 。所以，在 node 环境下即可：

```sh
npm install webpack webpack-cli --save-dev # 安装 webpack 及
webpack-cli
```

可使用以下命令测试安装：

```sh
npx webpack-cli -v # 查看 webpack-cli 版本信息
npx webpack -v  # 查看 webpack 版本信息
```

            